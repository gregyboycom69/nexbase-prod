Phase 22.12: Add Form Width/Height/AutoCenter properties. Popup respects the form's designed width/height.

CONTEXT: A SQL migration just added three columns to the pages table: width (INT, default 800), height (INT, default 600), and auto_center (BOOL, default true). We now need to (a) surface these in the Studio Property Sheet when a Form is selected, and (b) make PopupFormOverlay respect them instead of using a fixed 600px modal.

This commit touches exactly two files:
- src/app/(app)/studio/[slug]/page.tsx (Property Sheet UI)
- src/components/PopupFormOverlay.tsx (modal sizing)

Do NOT touch any other file.

PART A — Studio Property Sheet

STEP A1: Open src/app/(app)/studio/[slug]/page.tsx and find where the Property Sheet renders form-level properties on the Format tab (currently shows "Default View" and "Navigation Buttons" only, when Selection: Form is active). Search the file for the existing "Default View" or "Navigation Buttons" label to locate the block.

STEP A2: Add three new fields below the existing form-level properties on the Format tab:
- "Width" — number input bound to the active page's width. Default display: 800 if null. On change, update local state and persist to pages table.
- "Height" — number input bound to height. Default 600.
- "Auto Center" — dropdown with "Yes" / "No" options bound to auto_center boolean. Default "Yes" (true).

Use the EXACT same JSX styling/structure as the existing "Default View" and "Navigation Buttons" rows so the UI stays consistent.

STEP A3: When any of these fields change, save to the pages table via the existing update pattern. Match how "Default View" is saved today. Field names must match DB columns exactly: width, height, auto_center.

PART B — PopupFormOverlay respects form dimensions

STEP B1: Open src/components/PopupFormOverlay.tsx.

STEP B2: After the existing line:
  const popupPage = pages.find((p: any) => p.id === pageId)
Add:
  const popupWidth = popupPage?.width || 800
  const popupHeight = popupPage?.height || 600
  const popupAutoCenter = popupPage?.auto_center !== false

STEP B3: Find the inner modal div (the white card with maxWidth: 600, width: '100%', maxHeight: '90vh', padding: 32, borderRadius: 12). Replace its sizing styles:
REMOVE: maxWidth: 600, width: '100%', maxHeight: '90vh', minHeight: 400
REPLACE WITH: width: popupWidth, height: popupHeight, maxWidth: '95vw', maxHeight: '95vh'
Keep all other styles on that div exactly as they are.

STEP B4: The outer overlay div already centers the modal with display flex, alignItems center, justifyContent center. If popupAutoCenter is false, override alignItems and justifyContent to 'flex-start'. Keep everything else identical.

STEP B5: Do NOT change anything else:
- Do NOT modify the X button, ESC handler, backdrop click
- Do NOT modify NexBaseControl usage
- Do NOT modify loadPopupControls
- Do NOT touch /app/[slug]/page.tsx
- Do NOT touch NexBaseControl.tsx

BUILD

STEP C1: Run npm run build. If it succeeds, stop and report back. Do NOT commit. If errors appear, fix only the errors caused by this change. If TypeScript complains about width/height/auto_center not being on the page type, cast to any: (popupPage as any)?.width || 800