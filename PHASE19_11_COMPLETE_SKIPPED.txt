NexBase - Phase 19.11 - Complete the Skipped Fixes
Project: C:\Users\gregy\Projects\07_Next GS

═══════════════════════════════════════════════════════════════════
CRITICAL INSTRUCTIONS FOR CLAUDE CODE
═══════════════════════════════════════════════════════════════════

DO NOT SKIP ANY FIX IN THIS FILE.

If a fix seems "complex" - that means it NEEDS to be done.
If you previously skipped Fix 19.10.2 because it was complex,
you MUST do it now in Fix 19.11.1.

Take 30+ minutes per fix if needed. Do not rush.
Do not say "Skipped (Complex)". That is not acceptable.

If you genuinely cannot do a fix, explain SPECIFICALLY what 
prevents you, not just "complex". Show the user the exact 
code blocker.

═══════════════════════════════════════════════════════════════════
GOAL
═══════════════════════════════════════════════════════════════════

Complete the inline table designer that was skipped in 19.10.
Run the sanitization script that was created but not executed.
Verify all fixes actually take effect on the live site.

NO new features. NO redesign. Only finishing what was started.

═══════════════════════════════════════════════════════════════════
FIX 19.11.1 - INLINE TABLE DESIGNER (DO NOT SKIP)
═══════════════════════════════════════════════════════════════════

CURRENT BROKEN BEHAVIOR:
At /studio/ggg, when user clicks a table tab in sidebar 
(like "test" or "ttttt"), the canvas area shows blank with 
just text "table - test". User cannot edit fields.

To edit table, user must navigate AWAY to /studio/ggg/tables 
which is a separate dark-themed interface.

DESIRED BEHAVIOR:
At /studio/ggg, clicking a table tab shows the table designer 
INLINE in the canvas area, with the same theme as forms.
User never leaves the studio.

EXACT IMPLEMENTATION STEPS:

STEP 1 - Read the current studio file completely:
Open: src/app/(app)/studio/[slug]/page.tsx
Read the ENTIRE file. Understand:
- How activePage is set
- How activePage.type is checked  
- What renders for type='form' (existing form designer)
- What currently renders (or fails to render) for type='table'

STEP 2 - Read the existing tables page:
Open: src/app/(app)/studio/[slug]/tables/page.tsx
This is the separate dark-themed table designer that already 
works. Identify these key parts:
- The data fetching logic (loadTables, loadTableFields)
- The field grid render (Field Name, Data Type, Description rows)
- The save logic (saveFields function)
- The Field Properties panel at bottom
- Any state used (tableFields, selectedField, etc.)

STEP 3 - Check if a TableDesigner component exists:
Look at: src/components/TableDesigner.tsx
If yes, read it. We may be able to reuse it directly.

STEP 4 - Refactor approach (PICK ONE based on what you found):

OPTION A - If src/components/TableDesigner.tsx exists and is reusable:
In src/app/(app)/studio/[slug]/page.tsx, find the canvas area where 
form designer is rendered. Add conditional rendering:

{activePage?.type === 'form' && (
  <FormDesignerCanvas {...formProps} />
)}

{activePage?.type === 'table' && (
  <TableDesigner 
    workspaceId={workspaceId}
    tableId={activePage.id}
    tableName={activePage.name}
    theme="light"
  />
)}

Then ensure TableDesigner accepts a "theme" prop and applies 
light theme colors when set to "light".

OPTION B - If no reusable component exists:
Create new file: src/components/TableDesignerInline.tsx

Copy the table editing logic from /studio/[slug]/tables/page.tsx 
into this component. Adapt it to:
- Accept props: workspaceId, tableId, tableName  
- Use light theme colors (white bg, dark text, theme.ts values)
- Not include navigation header (it's inline)
- Not include left sidebar (parent already has one)
- Just show: table name, field grid, +Add Field, Save Table button

Then in studio page.tsx, render <TableDesignerInline /> when 
activePage.type === 'table'.

STEP 5 - Update the click handler for table tabs in sidebar:

Find the function that runs when a user clicks a table in the 
left sidebar (search for handleTableClick or similar).

If it currently does:
router.push(`/studio/${slug}/tables?table=${table.id}`)

CHANGE IT TO set activePage as the table:
const tablePage = {
  id: table.id,
  type: 'table',
  name: table.name,
  fields: table.fields,
}
setActivePageId(table.id)
setActivePage(tablePage)

This makes clicking a table tab keep user on /studio/ggg
and show the inline table designer.

STEP 6 - Test locally before committing:
Run: npm run dev
Open: http://localhost:3000/studio/ggg
- Click "test" table → should show inline table designer
- Click "Home" form → should switch to form designer
- Click "ttttt" table → should show inline table designer
- Click "+ Add Field" → adds a field to the table
- Type field name → no lag (uses defaultValue/onBlur)
- Click Save Table → saves to Supabase
- Refresh page → fields persist

Only commit if all 7 of these work locally.

STEP 7 - Commit and push:
git add -A
git commit -m "Phase 19.11.1: Inline table designer in studio"
git push origin main

DO NOT skip this fix. Take the time needed. This is the most 
important fix in the entire phase.

═══════════════════════════════════════════════════════════════════
FIX 19.11.2 - RUN THE SANITIZATION SCRIPT
═══════════════════════════════════════════════════════════════════

PROBLEM:
Phase 19.10.1 created scripts/sanitize-control-colors.ts but 
the script was never EXECUTED. So existing controls in 
Supabase still have bg:'#7f1d1d' and similar bad colors.

This is why textboxes still appear dark red even after the 
auto-sanitization-on-render code was added.

SOLUTION:

STEP 1 - Verify the script exists:
Check: scripts/sanitize-control-colors.ts
If missing, recreate it from PHASE19_10_POLISH.txt section 
"FIX 19.10.1 - SANITIZE EXISTING DATA IN SUPABASE"

STEP 2 - Verify env vars are present:
The script needs:
- NEXT_PUBLIC_SUPABASE_URL
- SUPABASE_SERVICE_ROLE_KEY

These should be in .env.local. Verify:
Get-Content .env.local | Select-String "SUPABASE"

If SUPABASE_SERVICE_ROLE_KEY is missing, the user needs to 
add it from Supabase dashboard (Settings > API > service_role).

STEP 3 - Update script to load env vars:
At top of scripts/sanitize-control-colors.ts add:
import { config } from 'dotenv'
config({ path: '.env.local' })

STEP 4 - Run the script:
npx tsx scripts/sanitize-control-colors.ts

Expected output:
Fixed page abc-123
Fixed page def-456
Total pages fixed: N

STEP 5 - Verify in Supabase:
Open Supabase dashboard, check pages table.
Pick a page that had dark red textboxes.
Look at controls JSONB column.
Verify bg values are now '#ffffff' or 'transparent' 
instead of '#7f1d1d'.

STEP 6 - Test on live site:
Open https://nexbase-inky.vercel.app/studio/ggg
Open the form that previously had dark red textboxes.
Verify they now appear white with dark text.

NO COMMIT NEEDED for this fix - it only modifies database, 
not code.

═══════════════════════════════════════════════════════════════════
FIX 19.11.3 - VERIFY BUTTON CONTRAST WORKS
═══════════════════════════════════════════════════════════════════

The previous button contrast fix may not be applied to 
existing buttons saved in Supabase.

STEP 1 - Find the button render code:
Search for: ctrl.type === 'Button' OR ctrl.type === "Button"
Likely in: src/app/(app)/studio/[slug]/page.tsx or 
src/app/app/[slug]/page.tsx

STEP 2 - Verify it uses getContrastText:
The button render should look like:

<button style={{
  background: ctrl.props?.bg || '#4f46e5',
  color: ctrl.props?.color || getContrastText(ctrl.props?.bg || '#4f46e5'),
}}>

If color uses ctrl.props?.color directly without fallback to 
getContrastText, that's the bug. Fix it to use the contrast 
function as fallback.

STEP 3 - Add to sanitization script:
In scripts/sanitize-control-colors.ts, the script should also 
fix button text color. Find the button section and ensure:

if (ctrl.type === 'Button') {
  const bg = newProps.bg || '#4f46e5'
  const isLightBg = isLightColor(bg)
  if (isLightBg && (!newProps.color || newProps.color === '#ffffff')) {
    newProps.color = '#1e293b'  
    modified = true
  }
  if (!isLightBg && (!newProps.color || newProps.color === '#000000' || newProps.color === '#1e293b')) {
    newProps.color = '#ffffff'
    modified = true
  }
}

STEP 4 - Re-run sanitization script:
npx tsx scripts/sanitize-control-colors.ts

STEP 5 - Test on live site:
Verify buttons with light backgrounds (like the green/orange 
SAVE buttons in screenshots) now have dark, readable text.

STEP 6 - Commit code changes if any:
git add -A
git commit -m "Phase 19.11.3: Button contrast for existing controls"
git push origin main

═══════════════════════════════════════════════════════════════════
FINAL TESTING CHECKLIST (RUN ALL BEFORE DECLARING DONE)
═══════════════════════════════════════════════════════════════════

Open https://nexbase-inky.vercel.app

[ ] Login works
[ ] Navigate to /studio/ggg  
[ ] Click "Home" form tab → form designer appears
[ ] Click "test" table tab → table designer appears INLINE
    (NOT blank, NOT navigation away)
[ ] Click "ttttt" table tab → table designer appears INLINE
[ ] Click "Home" form tab again → form designer reappears
[ ] All textboxes in forms appear WHITE (not dark red)
[ ] All buttons have READABLE text
[ ] Light theme is consistent throughout

If ANY of these fail, the phase is NOT complete.

═══════════════════════════════════════════════════════════════════
DECLARATION OF COMPLETION
═══════════════════════════════════════════════════════════════════

Only declare Phase 19.11 complete when ALL items above are 
verified working on the LIVE site (not just localhost).

If any fix could not be completed, state SPECIFICALLY:
- Which fix
- What exact code blocker prevented it
- What you tried
- What you recommend the user do manually

Do NOT just say "Skipped (Complex)". 
Be specific so the user can take action.

═══════════════════════════════════════════════════════════════════
GIT COMMIT RULES (REMINDER)
═══════════════════════════════════════════════════════════════════

- Single-line commit messages
- NO Co-Authored-By trailers (causes Vercel deploy issues)
- Commit after each fix, not all at end
- Push after each commit so Vercel deploys incrementally
